<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SocialLoginMaratona"
             x:Class="SocialLoginMaratona.Views.RootPage"
             Title="{Binding Title}">

    <ScrollView>
        <StackLayout Padding="60" VerticalOptions="CenterAndExpand">
            <Label Text="Id do usuário" TextColor="Black" FontSize="Small" />
            <Label Text="{Binding UserId}" TextColor="Black" FontSize="Medium" />
            <Label Text="Token do provedor" TextColor="Black" FontSize="Small" />
            <Label Text="{Binding Token}" TextColor="Black" FontSize="Medium" />
        </StackLayout>
    </ScrollView>


</ContentPage>